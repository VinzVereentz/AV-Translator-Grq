import express from 'express';import cors from 'cors';
const app=express();app.use(cors());app.use(express.json({limit:'1mb'}));
app.post('/translate',async(req,res)=>{try{if(!process.env.GROQ_API_KEY)return res.status(500).json({error:'GROQ_API_KEY belum diset'});const items=req.body?.items||[];if(!items.length)return res.status(400).json({error:'items required'});const r=await fetch('https://api.groq.com/openai/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${process.env.GROQ_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:req.body.model||'llama-3.3-70b-versatile',temperature:.2,response_format:{type:'json_object'},messages:[{role:'system',content:'Translate each Japanese subtitle to natural Indonesian. Return JSON object with key translations, an array of objects with id and translation. Preserve meaning and line breaks.'},{role:'user',content:JSON.stringify({items})}]})});const d=await r.json();if(!r.ok)return res.status(r.status).json(d);let content=d.choices?.[0]?.message?.content||'{}';const parsed=JSON.parse(content);res.json(parsed);}catch(e){res.status(500).json({error:e.message});}});app.get('/health',(_,res)=>res.json({ok:true}));app.listen(process.env.PORT||3000,()=>console.log('AV Translator backend running'));
